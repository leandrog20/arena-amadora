{
  "name": "arena-amadora",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,green \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "cd backend && npm install && npx prisma migrate deploy && npm run build && cd ../frontend && npm install && npm run build",
    "start": "concurrently -n backend,frontend -c blue,green \"npm run start --prefix backend\" \"npm run start --prefix frontend\"",
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "prisma:generate": "npm run prisma:generate --prefix backend",
    "prisma:migrate": "npm run prisma:migrate --prefix backend",
    "prisma:seed": "npm run prisma:seed --prefix backend"
  },
  "devDependencies": {
    "@prisma/prisma-schema-wasm": "^5.22.0-44.605197351a3c8bdd595af2d2a9bc3025bca48ea2",
    "concurrently": "^9.1.2",
    "prisma": "5.22.0"
  }
}
